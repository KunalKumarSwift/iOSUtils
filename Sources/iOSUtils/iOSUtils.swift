/// iOSUtils — a collection of commonly used iOS Swift utilities.
///
/// Public API surface is `iOSUtilsFacade.shared` for feature providers,
/// plus the extension files for value-level helpers on standard types.
/// Clients should import only this module — never sub-folders.
